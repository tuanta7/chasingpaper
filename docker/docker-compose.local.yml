services:
  postgres:
    image: postgres:18.0-alpine
    container_name: chasingpaper-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: chasingpaper
    restart: no